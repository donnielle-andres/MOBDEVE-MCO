<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/menu_parent"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MenuFragment">

    <ImageView
        android:id="@+id/menu_logo"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:src="@drawable/yardstick_logo"
        app:layout_constraintTop_toTopOf="@id/menu_parent"
        app:layout_constraintStart_toStartOf="@id/menu_parent"
        app:layout_constraintEnd_toEndOf="@id/menu_parent"/>

    <View
        android:id="@+id/menu_header_line"
        android:layout_width="match_parent"
        android:layout_height="2dp"
        android:background="#E91E63"
        app:layout_constraintStart_toStartOf="@id/menu_parent"
        app:layout_constraintTop_toBottomOf="@id/menu_logo"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view"
        app:layoutManager="LinearLayoutManager"
        android:scrollbars="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/menu_header_line"
        app:layout_constraintStart_toStartOf="@id/menu_parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>